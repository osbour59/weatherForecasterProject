For the CSS styles to load correctly, there is an if statement at the start of every webpage.
If dark mode is true, the dark css style is loaded, if false, the default one is loaded.
While this works, the webpages are prone to flickering due to having to pick the css style.